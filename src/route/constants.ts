export enum ROOT_ROUTES {
  HOME = "book",
  SALE = "sale",
  CUSTOMER = "cutm",
  SMS = "sms",
  MY_SHOP = "shop",
  SETTING = "setting",
  CS = "cs",
}

export enum HOME_ROUTES {
  CALENDAR = "calendar",
  TODAY_SALE = "today-sale",
}

export enum SALE_ROUTES {
  SHOP = "shop",
  CUSTOMER = "cutm",
  EMPLOYEE = "empl",
}

export enum CUSTOMER_ROUTES {
  MEMBERSHIP = "membership",
}

export enum SMS_ROUTES {
  SEND = "send",
  KAKAO = "auto-alarm",
  HISTORIES = "histories",
  RECHARGE = "recharge",
}

export enum MY_SHOP_ROUTES {
  COSMETIC = "cosmetic",
  PRODUCT = "product",
  EMPLOYEE = "empl",
  POINT = "point",
}

export enum SETTING_ROUTES {
  SHOP = "shop",
  NAVER = "naver",
  LOCK = "menu-lock",
}

export enum CS_ROUTES {
  NOTICE = "notice",
  EVENT = "event",
  QNA = "qna",
}
